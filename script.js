//your JS code here. If required.
var currentURL = window.location.href;

// Get the length of the URL
var urlLength = currentURL.length;

// Print the length of the URL using alert
alert("The length of the URL is: " + urlLength);
